#pragma once

#include <string>
#include <vector>


/**
 * Split a string based on a separator character (like split or explode in other languages)
 */
std::vector<std::string> split(const std::string& text, char separator);
